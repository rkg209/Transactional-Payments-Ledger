# Build output
target/
build/
!.mvn/wrapper/maven-wrapper.jar

# jOOQ generated sources — regenerated from the schema, never hand-edited,
# and therefore never committed. The schema (Flyway migrations) is the source
# of truth; the generated code is a build artifact of it.
src/main/java/org/ledger/db/generated/
generated-sources/

# IDE
.idea/
*.iml
*.iws
.vscode/
.settings/
.project
.classpath
nbproject/

# OS
.DS_Store
Thumbs.db

# Logs
*.log
logs/

# Env — never commit secrets
.env
.env.local

# Claude Code local overrides (`.claude/settings.json` IS committed on purpose:
# the guardrails and workflow are part of the deliverable)
.claude/settings.local.json
